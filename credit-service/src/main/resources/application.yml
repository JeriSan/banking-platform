server:
  port: 8083
spring:
  application:
    name: credit-service
  config:
    import: "configserver:http://localhost:8888"
  data:
    mongodb:
      database: creditdb
      uri: mongodb://localhost:27023/creditdb
management.endpoints.web.exposure.include: "*"
springdoc:
  swagger-ui:
    path: /swagger